import { MydropDirective } from './mydrop.directive';

describe('MydropDirective', () => {
  it('should create an instance', () => {
    const directive = new MydropDirective();
    expect(directive).toBeTruthy();
  });
});
